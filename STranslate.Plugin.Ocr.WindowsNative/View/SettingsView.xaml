<UserControl
    x:Class="STranslate.Plugin.Ocr.WindowsNative.View.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:vm="clr-namespace:STranslate.Plugin.Ocr.WindowsNative.ViewModel"
    d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <ikw:SimpleStackPanel Spacing="12">

        <!--  OCR 引擎状态  -->
        <ui:SettingsCard Header="{DynamicResource STranslate_Plugin_Ocr_WindowsNative_Status}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Info_20_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <ikw:SimpleStackPanel Orientation="Vertical" Spacing="6">
                <TextBlock Text="{DynamicResource STranslate_Plugin_Ocr_WindowsNative_Status_Description}" TextWrapping="Wrap" />
                <ikw:SimpleStackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock FontSize="{DynamicResource STControlFontSize12}"
                               Foreground="{DynamicResource {x:Static ui:ThemeKeys.SystemFillColorNeutralBrushKey}}"
                               Text="{DynamicResource STranslate_Plugin_Ocr_WindowsNative_EngineLanguage_Label}" />
                    <TextBlock FontSize="{DynamicResource STControlFontSize12}"
                               Text="{Binding EngineLanguage}" />
                </ikw:SimpleStackPanel>
            </ikw:SimpleStackPanel>
        </ui:SettingsCard>

        <!--  OCR 语言选择  -->
        <ui:SettingsCard
            Description="{DynamicResource STranslate_Plugin_Ocr_WindowsNative_Language_Description}"
            Header="{DynamicResource STranslate_Plugin_Ocr_WindowsNative_Language}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Translate_20_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <ikw:SimpleStackPanel Orientation="Horizontal" Spacing="8">
                <ComboBox
                    MinWidth="200"
                    DisplayMemberPath="DisplayName"
                    ItemsSource="{Binding AvailableLanguages}"
                    SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}" />
                <Button
                    Command="{Binding RefreshLanguagesCommand}"
                    ToolTip="{DynamicResource STranslate_Plugin_Ocr_WindowsNative_Refresh}">
                    <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.ArrowSync_20_Regular}" />
                </Button>
            </ikw:SimpleStackPanel>
        </ui:SettingsCard>

        <!--  使用次数  -->
        <ui:SettingsCard Header="{DynamicResource STranslate_Plugin_Ocr_WindowsNative_UseCount}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.DataUsage_20_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <TextBlock>
                <Run Text="{DynamicResource STranslate_Plugin_Ocr_WindowsNative_UseCount_Format1}" />
                <Run Text="{Binding UseCount}" />
                <Run Text="{DynamicResource STranslate_Plugin_Ocr_WindowsNative_UseCount_Format2}" />
            </TextBlock>
        </ui:SettingsCard>

    </ikw:SimpleStackPanel>
</UserControl>
